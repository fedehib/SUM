@model IEnumerable<SUM.Models.Contacto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Rol
        </th>
        <th>
            Nombre
        </th>
        <th>
            Teléfono
        </th>
        <th>
            Consorcio
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tx_rol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tx_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tx_telefono)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Consorcio.tx_descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new {  id=item.cd_contacto}) |
            @Html.ActionLink("Detalles", "Details", new { id =item.cd_contacto }) |
            @Html.ActionLink("Eliminar", "Delete", new { id = item.cd_contacto })
        </td>
    </tr>
}

</table>
