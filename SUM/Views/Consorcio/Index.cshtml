@model IEnumerable<SUM.Models.Consorcio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Descripción
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tx_descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.cd_consorcio }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.cd_consorcio }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.cd_consorcio })
        </td>
    </tr>
}

</table>
